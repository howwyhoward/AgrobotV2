---
description: Agrobot TOM v2 project context, hardware environments, and sprint state
alwaysApply: true
---

# Project: Agrobot TOM v2
Industry-grade autonomous perception pipeline for a tomato-picking robot. Bazel monorepo + ROS 2 Jazzy.

## Hardware Environments
Always label commands and environment-specific decisions with one of these tags:

| Tag | Hardware | Accelerator | Role |
|-----|----------|-------------|------|
| `[MAC]` | Local Mac (Apple Silicon) | Apple MPS | Infra, prototyping, code writing |
| `[NUCBOX]` | GMKtec EVO-X2, 96GB | ROCm/MIGraphX | Training + real-time inference |

## Current Sprint
<!-- UPDATE THIS AS SPRINTS ADVANCE -->
**Phase 3 — Sprint 3: Done. Starting Sprint 4.**
Sprint 3 complete: ONNX export, SAM2 fine-tuning, SAM2 AMG detector (mAP 0→0.023), failure modes + watchdog.
MIGraphX GPU benchmark deferred (ROCm/kernel conflict) — see docs/SPRINT3_ROCM_ISSUE.md.
Full step-by-step plan: [docs/GRAND_PLAN.md](docs/GRAND_PLAN.md)

## Sprint Roadmap
- Sprint 1: Bazel monorepo · Docker ROS 2 Jazzy · Tailscale · RealSense · 2-machine stack ✅
- Sprint 2: DINOv2+SAM2 zero-shot · eval (mAP, latency) · depth→3D · CI · ablation · SAM2 fine-tune ✅
- Sprint 3: ONNX export · SAM2 AMG detector · SAM2 fine-tuning · failure modes + watchdog ✅ · MIGraphX GPU ⏳ (ROCm 7.3)
- Sprint 4: Quantized Qwen-VL · HIL testing `[NUCBOX]`

## Key Architectural Patterns Already Established
- **Thin node, thick library**: ROS 2 nodes subscribe/publish only; all logic lives in utility modules
- **Pluggable detector interface**: `PlaceholderDetector` → `DINOv2SAM2Detector` → `SAM2AMGDetector` (zero node changes)
- **SAM2 proposes, DINOv2 scores**: architecturally correct — SAM2 AMG generates pixel-precise masks, DINOv2 scores for tomatoness
- **Sprint-forward comments**: `# Sprint N: replace with ...` marks all known future swap points
- **apt over pip** for anything the ROS base image already ships (numpy, opencv, pytest, rclpy)

## Employer-Grade Gaps (addressed in grand plan)
- Evaluation → Sprint 2: eval script, mAP/latency, REPRODUCE.md
- 2D only → Sprint 2: depth→3D per detection
- No CI → Sprint 2: .github/workflows/ci.yml
- No technical hook → Sprint 2: one ablation (DINOv2-only vs DINOv2+SAM2)
- Safety/fallback → Sprint 3: docs/FAILURE_MODES.md + code behavior
