# Bazel
bazel-*
.bazel/

# Disk cache (can be large — don't commit it)
~/.cache/bazel/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# Build Event Protocol output
/tmp/agrobot_bep.json

# Editor
.idea/
.vscode/
*.swp

# Docker Compose env (machine-specific paths — copy .env.example to .env)
.env

# Model weights (large binary files — use Git LFS or model_sync.sh instead)
models/*.onnx
models/*.pt
models/*.pth
models/*.bin
models/*.safetensors
!models/.gitkeep

# SSH control sockets
~/.ssh/cm-*

# OS
.DS_Store
